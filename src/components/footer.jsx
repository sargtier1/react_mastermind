import React from 'react'

import styled from 'styled-components'

const Footer = () => {
  return <StyledFooter>This was made with love + react</StyledFooter>
}

const StyledFooter = styled.footer`
 
`

export default Footer
